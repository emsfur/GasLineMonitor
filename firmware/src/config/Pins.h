#pragma once

#define MAX_DISTANCE 3 // defines max distance (cm) to ping for [anything above max will resolve to 0]

// program only manages one sensor currently
#define TRIG_PIN 2
#define ECHO_PIN 32